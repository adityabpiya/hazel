<% @items.each do |item| %>
    <div class="booyah-box col-10 offset-1">
        <h1><%= item.name %></h1>
        <i><%= item.description %></i><br /><br />
        <h2><%= item.cost %></h2><br />
    </div>

    
<% end %>